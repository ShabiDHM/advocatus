# FILE: Caddyfile
# PHOENIX PROTOCOL - THE DEFINITIVE AND FINAL CORRECTION (WEBSOCKET AUTHENTICATION)
# CORRECTION: Added 'header_up Sec-WebSocket-Protocol' to the WebSocket reverse_proxy.
# This explicitly forwards the authentication token from the client to the backend,
# resolving the handshake failure. This is the definitive fix for the WebSocket
# connection error.

advocatus-prod-api.duckdns.org {
    tls shabanbala@gmail.com

    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        X-Content-Type-Options "nosniff"
        X-Frame-Options "SAMEORIGIN"
    }

    route {
        # First, specifically match WebSocket requests.
        reverse_proxy /ws/* backend:8000 {
            # Apply the necessary headers to upgrade the connection.
            header_up +Connection {http.request.header.Connection}
            header_up +Upgrade {http.request.header.Upgrade}
            # CRITICAL: Forward the authentication token header to the backend.
            header_up Sec-WebSocket-Protocol {http.request.header.Sec-WebSocket-Protocol}
        }

        # Second, handle all other requests as a standard reverse proxy.
        reverse_proxy backend:8000
    }
}